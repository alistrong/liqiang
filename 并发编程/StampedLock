StampedLock 

在使用锁的方面：1 使用了一个票据(stamp)的概念，加锁解锁的时候，当做一个门票，如果传递错误门票可能抛异常或者其他问题
              2 是不可重入的，可能会产生死锁，所以写代码应该注意。
乐观锁： 由于大部分场景读操作不会与写操作竞争，因此使用读锁有点太重了，一个更好的方法就是继续执行读，结束的时候判断该值是否修改，如果修改重试，获取升级锁

相对于readwritelock，stampedlock的读操作可以依靠乐观读的策略，类似无锁操作，从而不会阻塞写线程
